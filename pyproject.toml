[project]
name = "award-books-tracker"
version = "0.1.0"
description = "Track sci-fi/fantasy award winners against Goodreads and StoryGraph reading history"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.2",
    "rapidfuzz>=3.9",
    "httpx>=0.27",
]

[tool.ruff]
line-length = 100
